let radius=10

function areaOfCircle(radius){
    return Math.PI* Math.pow(radius,2)
}

console.log(`The area of circle with the radius-${radius} is : ${areaOfCircle(radius)}`);

// O/P :
// The area of circle with the radius-10 is : 314.1592653589793
